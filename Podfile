# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'
install! 'cocoapods', :warn_for_unused_master_specs_repo => false

target 'ProgrammaticTemplate' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'RxSwift', '6.5.0'
    pod 'RxCocoa', '6.5.0'
  # Pods for ProgrammaticTemplate

  target 'ProgrammaticTemplateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ProgrammaticTemplateUITests' do
    # Pods for testing
  end

end
